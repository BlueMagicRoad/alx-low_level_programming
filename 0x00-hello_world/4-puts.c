#include<stdio.h>
/**
 * The starting point of the program is main
 *
 * It will return 0 if run successfully , and a non-zero value if not
 */
int main(void)
{
	char task1[] = "\"Programming is like building a multilingual puzzle";

	puts(task1);
	return (0);
}
